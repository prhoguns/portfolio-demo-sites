const form=document.querySelector('form');if(form)form.addEventListener('submit',e=>{e.preventDefault();form.querySelector('.form-note').textContent='Thanks. We will email you shortly to confirm your appointment.';form.reset()});const menuButton=document.querySelector('.menu');if(menuButton)menuButton.addEventListener('click',()=>document.querySelector('nav').classList.toggle('open'));
