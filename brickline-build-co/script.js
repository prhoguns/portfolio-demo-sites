const form=document.querySelector('form');form.addEventListener('submit',e=>{e.preventDefault();form.querySelector('.form-note').textContent='Thanks. We will review the details and call you within two business days.';form.reset()});document.querySelector('.menu').addEventListener('click',()=>document.querySelector('nav').classList.toggle('open'));
